# Minimal Desktop integration proof

Prepared 2026-09-21 for the Gate 5-approved first implementation slice. **Desktop execution: Pass**, reported by the manager and corroborated on 2026-09-23 UTC. [Retained evidence](../evidence/desktop-proof-validation.md) records both refreshes, matching values, saved-file observations and actual cloud jobs. This is a synthetic connectivity/calculation proof, not final report acceptance. Gates 6–9 remain. The steps below are the executed procedure; no repeat is requested now. Existing `powerbi-files/` artifacts are preserved.

The [cloud proof](../evidence/phase-6-cloud-proof.md) passed and [opp02-proof.pbip](../../powerbi/opp02-proof/opp02-proof.pbip) remains bound to batch `fix01_5f624cd96d7420ac70f1d04569c97efa81697391353f6d455d5ec1dd1445c2cc` in its [model parameter](../../powerbi/opp02-proof/opp02-proof.SemanticModel/definition/expressions.tmdl). Credential containment for [SEC-001](../evidence/security-evidence.md) and subsequent renewed sign-in are recorded separately. The completed host proof permits the next approved backend slice; it does not approve Gate 6 or broader report behavior.

## Agent handoff prerequisites

Before asking the manager to refresh, record the successful cloud publication and exact batch, confirm billing/runtime/cost conditions and report-user access, and check the remaining [envelope](execution-envelope.md). Source and job-billing project must both be **`astra-po-lab-20260921`**; source dataset is **`po_serving` in `us-east4`**. The proof pins ADBC **Implementation `2.0`**, Import and Storage API. Its two native source queries read only `batch_manifest` and `po_line_snapshot` for the same batch. `event_evidence` remains a backend publication requirement but is not imported in this first proof.

The manifest must have exactly one `READY`/`PASS` FIX-01 row and ten published lines; the model checks manifest count/readiness, snapshot count and unique line IDs before importing. Publication and these guards do not replace independent backend reconciliation. Regional table placement is established by deployment and must be confirmed in resulting Desktop job evidence; no unsupported M location option is invented. Retain explicit job project/location/bytes after refresh. Desktop can generate additional metadata/preview jobs; two source queries are not a promise of exactly two billed jobs.

## Three manager steps

1. **Open and authenticate.** Close any earlier copy of this proof, then open the PBIP above in the already identified August 2026 x64 Desktop. Keep the existing report untouched. If prompted, use the approved manager Google sign-in. Review native-query prompts: both statements must be read-only `SELECT` against the two named lab tables with the supplied batch filter. Do not publish, create a billing account, change projects, grant permissions, enable a fallback, or accept creation of `_bqadbc_temp_tables`/other resources to clear an error. Stop and retain the sanitized error if those are requested.
2. **Refresh and inspect.** Use **Home → Refresh** once, counting any automatic initial import as this first attempt. The top visual must show the supplied batch ID, source as-of **2026-09-21 12:00:00Z**, the observed import timestamp and an explicit synthetic freshness state. At or after the source time, the summary must show **48 remaining units, $144.00 overdue value, 10 lines**. Check line contributions: **L01 6/$60, L07 4/$28, L08 4/$24, L09 4/$32**; L06 is **15/$0** after its promise extension; closed L04 is **0/$0**. A real clock before source-as-of must show **Invalid clock context** and blank totals. Above 24 hours, **Stale synthetic snapshot** is correct; refreshing does not make the historical source fresh. Do not modify the system clock or source dates to obtain a pass.
3. **Save, reopen, repeat and return evidence.** After a successful first import, save this separate proof, close it, reopen the same PBIP and confirm the page, batch and imported values persist. Perform the **second refresh** and confirm the same batch/totals with a new actual import time; this is QA-07's required repeat-refresh check. Count automatic imports, previews and failed refreshes toward the **two-attempt daily limit**. If that limit has already been used or the first attempt fails, stop and report the result; never add a third attempt. Record summary/context and line values without account identifiers, pass/fail, both actual refresh times, reopen time and any sanitized error. The agent retains resulting job project/region/bytes and Desktop-saved changes. A failed refresh can leave old cached values visible: it is a failed attempt, never evidence of a new successful import. If the second refresh cannot run within the envelope, that subcheck remains Not Run.

No version/About or first-proof question is outstanding. The manager executed the host checks; the agent did not directly control or observe Desktop. Exact human refresh timings remain unmeasured; cloud job timestamps and filesystem save times are retained with their different meanings. Broader filters, event drilldown, all KPI boundaries, buyer tasks and final report styling remain in their approved later steps.

## File and validation notes

The proof consists of one Import business table and three plain table visuals: context, totals and line contributions. Currency is summed in integer cents before display. `pBatchId` is the only batch selection point. Import time comes from Power Query's observed UTC clock, not the backend publication timestamp; the source as-of remains unchanged. The model deliberately restricts this first slice to FIX-01 and does not claim support for empty or corrected batches yet.

`node powerbi/opp02-proof/validate-metadata.mjs` checks local references, visual field bindings, encoding and declared connector/batch/manifest/freshness guards. [JSON schema results](../../powerbi/opp02-proof/metadata-validation.json) record **ten passing** metadata files checked with Ajv 8.17.1/ajv-formats 3.0.1 against Microsoft's public schemas, including content hashes. Validation tooling was temporary and did not add a product dependency. **Neither check parses/executes TMDL, M or DAX in Desktop, verifies credentials, runs BigQuery, renders visuals or proves save/reopen.** Host results remain Not Run until step 3 evidence is retained.

Microsoft documents the [PBIR folder/reference format](https://learn.microsoft.com/en-us/power-bi/developer/projects/projects-report), [TMDL syntax](https://learn.microsoft.com/en-us/analysis-services/tmdl/tmdl-overview), [BigQuery connector settings](https://learn.microsoft.com/en-us/power-query/connectors/google-bigquery) and [native-query execution](https://learn.microsoft.com/en-us/power-query/native-query-folding). These establish the authoring basis; actual installed-host compatibility remains the purpose of this proof. Close Desktop before external edits and reopen afterwards. Local credentials, `.pbi` settings/cache and recovery files remain outside source control.
