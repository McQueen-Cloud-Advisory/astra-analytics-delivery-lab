# Environment readiness

**End-of-day update, September 23:** work is paused at the manager's request. [Runtime delta](evidence/phase-6-incremental-cloud-validation.md), [17 data cases and duplicate replay](evidence/phase-6-boundary-validation.md), and [metadata-only byte-cap reconciliation](evidence/phase-6-limit-probe-reconciliation.json) now pass their stated checks; the original suite failure remains recorded. [Latest inventory](evidence/phase-6-validation-post-run.json) shows the same five tables, unchanged expiry and 740 MiB explicitly reported cumulative query usage. All three Pacific September 23 attempts are consumed. Current local tests pass 177/177. Subsequent [publication and matching CI](evidence/ci-run-2026-09-23-platform.json) for implementation commit `acdb6d3` passed 177/177 tests and all checks. This update supersedes earlier pending runtime/boundary and September22 daily-status statements below; see [project state](project-state.md) for resume instructions.

Version: 1.5. Updated 2026-09-22 America/Chicago during Phase 6. This inventory distinguishes user-reported availability, observable tooling and working authenticated integrations. Authority is recorded at [Gate 4](gates/gate-4-architecture.md) and [Gate 5](gates/gate-5-data-design.md); this inventory alone authorizes no action.

Planning update, DEC-SCOPE-001 on 2026-09-20: the manager reports existing GCP with approximately $30/month allowance and confirms that the Power BI report will not be published. This resolves account-availability/publication-scope assumptions; it is not a new API or host test. GCP is the primary incremental cost focus; standing ChatGPT Pro is excluded from project cost.

Historical Phase 4 clarification established a new project, East US, unused reported allowance and installed Desktop. Gate 4 selects us-east4 and short demonstration operation. **Gate 5 is approved unchanged.** The manager resolved billing linkage, reauthenticated and confirmed at least $15 headroom; [cloud-control evidence](evidence/cloud-controls.json) records verified pricing, budget, quota and runtime metadata access. Two datasets/five tables are provisioned. The [minimal Desktop proof](evidence/desktop-proof-validation.md) and [FIX-02 correction, bounded recovery and no-op](evidence/phase-6-correction-validation.md) pass; complete platform and report acceptance remain open. All three cloud attempts for Pacific September 22 are consumed.

| Capability | Status | Observed evidence | Consequence / next verification | Owner / due point |
|---|---|---|---|---|
| Workspace and Git | Verified; exact authorized snapshot pushed | Initial inventory: Git 2.49.0.windows.1, HEAD `92f4bad`, tracked README/LICENSE/.gitignore. Later snapshot `8e9649e` was published to `codex/phase6-platform-proof` after authorized audit/findings review | User artifacts are preserved; [current CI evidence](evidence/ci-validation.md) separates the 143-file public snapshot from later uncommitted/unpushed changes and retains the earlier push rejection | Agent / current |
| Local shell and Node | Verified | PowerShell 5.1.26100.9444; Node v24.15.0; [110 current local tests pass](evidence/ci-validation.md) | Node 24 is the approved runner; local tests do not prove cloud or CI execution | Agent / current |
| Python invocation | Unavailable | `python` absent from PATH; `py --version` reports “No installed Python found!”; status applies to these checked commands | Select/verify an implementation runtime after selection; other installations were not exhaustively searched | Agent proposes; manager handles needed installation/auth / Gate 4 |
| GCP CLI and BigQuery CLI | Tooling and renewed scoped access verified | SDK 585.0.0, bq 2.1.38; original revocation/sign-in history retained in [security evidence](evidence/security-evidence.md). Latest renewed sign-in and [safe read-only check](evidence/desktop-cloud-observation-2026-09-23.json) passed at 2026-09-23T02:35:33.487Z, followed by scoped FIX-02 workloads | Explicit lab target only; deprecated `financial-analytics-demo` prohibited. Future workloads require a new quota day and fresh readiness checks | Agent / before each scoped action |
| GCP project, billing and controls | Verified for bounded first slice | `astra-po-lab-20260921` / 329955978985; billing enabled/open USD account ending BA6E; manager confirms at least $15 headroom; us-east4 rates verified; $10 project alerts-only budget and effective 10,240 MiB/day quota configured | Original BE9B quota failure is historical. No agent-created billing account. Invoice not inspected; alerts/quota are not a monetary hard cap. Review costs and delayed usage within the envelope | Manager and agent / before each session |
| Scoped pipeline identity | Metadata and bounded workload access verified | `po-pipeline` keyless impersonation; project Job User, Data Editor only on work/serving datasets; manager Token Creator only on that service account; zero user-managed keys | Explicit lab project/quota context, no shared ADC or Owner fallback. FIX-01/FIX-02 workload results and separate human Desktop proof do not establish all acceptance cases | Agent / current |
| BigQuery resources | Two datasets/five tables provisioned and read back | `po_work` and `po_serving`, us-east4, logical billing; exact schemas, grants and fixed table expiry verified in local `.lab/resource-manifest.json` | Conservative first-resource clock 2026-09-22T02:26:29.341Z; raw expiry Sep29, absolute serving/cleanup backstop Oct6 at same UTC time; repeat runs cannot reset it | Manager and agent / ongoing until cleanup |
| Cloud first slice | PASS on second attempt; first failure retained | Completed 2026-09-22T02:36:20.527Z: ten lines/ten events published and reconciled, zero oracle differences, 52,428,800 billed query bytes. First exit1 remains ABORTED_PROCESS_EXIT with reservation retained | [Cloud evidence](evidence/phase-6-cloud-proof.md) separates observed bytes from roughly $0.000298 gross query estimate; no invoice claim. Those two attempts belong to Pacific September 21; subsequent Desktop and FIX-02 results are recorded separately | Agent / first slice complete; broader acceptance pending |
| Credential containment | Revocation, renewed sign-in and safe validation complete | Diagnostic403 exposed temporary token in tool output; no repository credential file written. Authorized revocation verified, manager signed in again, scoped read-only check passed at 02:48:18.909Z; no old-token reuse probe | [Security evidence](evidence/security-evidence.md) retains the incident and the later authorized audit with zero known vulnerabilities. This is no clean-security claim; current working-tree CI coverage remains incomplete | Agent / security follow-up |
| FIX-02 recovery, replay and current inventory | Bounded recovery and verified no-op Pass | [Correction evidence](evidence/phase-6-correction-validation.md): retained FIX-01 stays at $144; FIX-02 reconciles at 48 remaining/$140. Same two datasets/five tables now hold raw20/22, serving20/22 and two manifests, with unchanged expirations | All three Pacific September 22 attempts consumed, including the retained EPERM failure; no fourth attempt. Cumulative query usage350 MiB is an observed byte total, not an invoice. [Initial metadata](evidence/cloud-proof.json) remains historical | Agent / observed through 2026-09-23T02:50:40.289Z |
| Terraform | Not Yet Verified | Binary reports v1.11.4; reading user configuration directory denied | Presence alone does not prove working infrastructure tooling; choose IaC proportionately at architecture | Agent / Gate 4 |
| GitHub repository read access | Verified by initial metadata check | GitHub connector returned metadata for `McQueen-Cloud-Advisory/astra-analytics-delivery-lab`, default branch `main`, visibility public, pull/push permissions reported | That initial check made no mutations; subsequent Git/CI activity belongs to [current CI evidence](evidence/ci-validation.md) | Agent / current |
| GitHub CLI / CI execution | Authorized snapshot push and actual CI Pass | [Current CI evidence](evidence/ci-validation.md): exact commit `8e9649e4fda23196e51491fad05644cbb765d0ce`, run `35816243301`, completed 2026-09-23T03:55:32Z with 98/98 tests and all workflow checks passing | The manager's audit/findings-review condition was satisfied; historical automatic-review rejections remain recorded. Later 110-test local composition changes are uncommitted/unpushed and have no CI result. No main merge or release | Agent / Gate 6 |
| Existing Power BI project source | Verified as files only | User-provided `powerbi-files/placeholder-report.pbip`, PBIR report, TMDL model and one page; parsed metadata and source inspected | Empty starting model has no demonstrated source connection, measures, or report behavior; preserve it | Agent / current |
| Power BI Desktop/runtime | Identified host and minimal integration Pass | User supplied August 2026 **2.157.1354.0 (26.08), x64** About details at 2026-09-21T03:46:04.6414546Z. Enabled: `PBI_gitIntegration`, `PBI_tmdlInDataset`, `PBI_enhancedReportFormat`, `PBI_enhancedReportFormatPBIX`; BigQuery legacy ODBC flag disabled. [Later host evidence](evidence/desktop-proof-validation.md) confirms both refreshes at 48/$144/10 and save/reopen | C3-02 and the minimal integration condition are closed; broader report acceptance remains due. Exact human refresh timings are unmeasured | Manager, approved host verifier / minimal proof complete |
| Power BI Service publication, capacity and workspace | N/A for current scope | Manager explicitly says the report will not be published, DEC-SCOPE-001; no service inspection needed for this endpoint | Exclude publication/capacity purchases; the completed local proof does not authorize publication or final report acceptance | Agent retains scope; manager for any later change |
| New minimal Power BI proof source | Minimal host proof Pass | Manager confirmed both refreshes displayed 48/$144/10 including save/reopen; Desktop-added metadata and retained source bindings corroborate round-trip | [Desktop evidence](evidence/desktop-proof-validation.md); original metadata schema check remains historical, and a cache-excluding validator checks current source. Preserve user saves | Manager + agent / minimal proof closed |
| End-to-end BigQuery → Power BI behavior | Minimal FIX-01 proof Pass | Ten-line cloud slice plus actual host result and 18 successful connector queries in approved project/us-east4; pinned batch and no extra named resource | FIX-02 backend checks have since passed, but the report remains pinned to FIX-01. Broader filters, stale/error cases, buyer tasks and final report checks remain open | Agent and manager / Gate 6 in progress |
| Docker executable | Found; runtime Not Yet Verified | Phase 4 Get-Command found Docker executable; daemon/container execution not tested | Does not require adopting a container route; current proposal uses Node locally | Agent / only if alternative selected |
| Native app control | Unavailable through current helper | Computer-use skill initialization imported successfully; `sky.list_apps()` failed: native pipe unavailable, OS error 2 | Cannot inspect Desktop version/render from this session; manager supplies host facts/evidence. No broad claim that Desktop/native APIs are absent | Manager for host checks; agent records limit |
| Browser control for cloud preflight | Unavailable in current session | `cua.getBrowser` for Cloud Console returned “No browser is available” | No console access/authentication/billing check occurred. Manager console or a supported authenticated CLI/API path is needed | Manager and agent / C3-01 |

Gate 5 is **Approved unchanged**, DEC-G5-001; C3-04 closes on the approved baseline. Verified billing/control configuration and manager-confirmed headroom supported the scoped-runtime cloud work. Minimal host proof, bounded FIX-02 correction/recovery/no-op, authorized dependency audit and exact-snapshot CI pass. Runtime retained-history delta admission, remaining boundary/invalid cases and CI coverage of later local changes still prevent a Gate 6 recommendation. The conventional ADC file is not used by this runtime; its earlier uninspected contents are not a prerequisite for explicit CLI impersonation. [Bootstrap evidence](evidence/cloud-bootstrap.md), [cloud controls](evidence/cloud-controls.json) and [first-slice evidence](evidence/phase-6-cloud-proof.md) preserve their historical observations.

The token-exposure stop was addressed by explicitly authorized revocation, manager sign-in and safe scoped validation. No old-token reuse probe was made, and the historical exposure remains recorded. Current work is local while the Pacific September 22 cloud attempt allowance is exhausted; future workloads require a new quota day and fresh readiness checks. The later audit/publication authorization and passing results supersede the prior transfer blockers for that exact snapshot only. They authorize no additional GCP workload or publication of subsequent local changes. Original resource/cost limits continue to apply.

The sole Human Manager is the approved host verifier under charter v1.1 and has completed the recorded minimal proof. That result is not final report acceptance. Native/browser helper limitations remain separate from the manager's actual host evidence.

Only relevant version/format facts from the user diagnostic are retained here. The full telemetry block, user/session identifiers and trace paths are unnecessary for the public repository and are not included. Although the copied block carried a Frown (Error) label, the manager clarified it came from About after normal Desktop launch, not from opening the placeholder, and **no error messages occurred**. No report defect or repair is inferred.

Command failures and connector limitations are retained as observations; they are not technical interventions by the manager. The original preflight made no cloud mutation. Later approved work created the dedicated project, scoped identity, controls and two datasets/five tables, then published/reconciled FIX-01 and FIX-02. The initial billing failure, unexplained first process exit and later repaired local journal failure remain historical evidence; the manager's billing/authentication/headroom actions are not technical corrections. A successful bounded retry does not establish the cause of an earlier failure. Desktop host actions are manager-confirmed; no agent-controlled Desktop/browser session is asserted.
