# Environment readiness

Version: 1.3. Updated 2026-09-21 America/Chicago during Phase 6's first implementation slice. This inventory distinguishes user-reported availability, observable tooling and working authenticated integrations. Authority is recorded at [Gate 4](gates/gate-4-architecture.md) and [Gate 5](gates/gate-5-data-design.md); this inventory alone authorizes no action.

Planning update, DEC-SCOPE-001 on 2026-09-20: the manager reports existing GCP with approximately $30/month allowance and confirms that the Power BI report will not be published. This resolves account-availability/publication-scope assumptions; it is not a new API or host test. GCP is the primary incremental cost focus; standing ChatGPT Pro is excluded from project cost.

Historical Phase 4 clarification established a new project, East US, unused reported allowance and installed Desktop. Gate 4 selects us-east4 and short demonstration operation. **Gate 5 is approved unchanged.** The manager resolved billing linkage, reauthenticated and confirmed at least $15 headroom; [cloud-control evidence](evidence/cloud-controls.json) records verified pricing, budget, quota and runtime metadata access. Two datasets/five tables are provisioned and the first cloud slice passed on its second attempt. Actual report behavior remains **Not Run**. Desktop build/bitness is already supplied.

| Capability | Status | Observed evidence | Consequence / next verification | Owner / due point |
|---|---|---|---|---|
| Workspace and Git | Verified | Local Git 2.49.0.windows.1; HEAD `92f4bad`; tracked baseline README/LICENSE/.gitignore | User's untracked prompt, review, and Power BI files preserved; new work remains local | Agent / current |
| Local shell and Node | Verified | PowerShell 5.1.26100.9444; Node v24.15.0; local implementation/metadata checks executed | Node 24 is the approved runner; local tests do not prove cloud execution | Agent / current |
| Python invocation | Unavailable | `python` absent from PATH; `py --version` reports “No installed Python found!”; status applies to these checked commands | Select/verify an implementation runtime after selection; other installations were not exhaustively searched | Agent proposes; manager handles needed installation/auth / Gate 4 |
| GCP CLI and BigQuery CLI | Tooling and renewed scoped access verified | SDK 585.0.0, bq 2.1.38; authorized revocation at 2026-09-22T02:43:12.7747162Z followed by manager sign-in. Safe read-only access check passed at 02:48:18.909Z | No new sign-in request outstanding. Explicit lab target only; deprecated `financial-analytics-demo` prohibited | Agent / before each scoped action |
| GCP project, billing and controls | Verified for bounded first slice | `astra-po-lab-20260921` / 329955978985; billing enabled/open USD account ending BA6E; manager confirms at least $15 headroom; us-east4 rates verified; $10 project alerts-only budget and effective 10,240 MiB/day quota configured | Original BE9B quota failure is historical. No agent-created billing account. Invoice not inspected; alerts/quota are not a monetary hard cap. Review costs and delayed usage within the envelope | Manager and agent / before each session |
| Scoped pipeline identity | Metadata and first-slice workload access verified | `po-pipeline` keyless impersonation; project Job User, Data Editor only on work/serving datasets; manager Token Creator only on that service account; zero user-managed keys | Explicit lab project/quota context, no shared ADC or Owner fallback. Broader acceptance and separate Desktop identity behavior remain to be tested | Agent / current |
| BigQuery resources | Two datasets/five tables provisioned and read back | `po_work` and `po_serving`, us-east4, logical billing; exact schemas, grants and fixed table expiry verified in local `.lab/resource-manifest.json` | Conservative first-resource clock 2026-09-22T02:26:29.341Z; raw expiry Sep29, absolute serving/cleanup backstop Oct6 at same UTC time; repeat runs cannot reset it | Manager and agent / ongoing until cleanup |
| Cloud first slice | PASS on second attempt; first failure retained | Completed 2026-09-22T02:36:20.527Z: ten lines/ten events published and reconciled, zero oracle differences, 52,428,800 billed query bytes. First exit1 remains ABORTED_PROCESS_EXIT with reservation retained | [Cloud evidence](evidence/phase-6-cloud-proof.md) separates observed bytes from roughly $0.000298 gross query estimate; no invoice claim. Two of three daily cloud attempts consumed. No backend expansion before Desktop proof | Agent / first slice complete; broader acceptance pending |
| Credential containment | Revocation, renewed sign-in and safe validation complete | Diagnostic403 exposed temporary token in tool output; no repository credential file written. Authorized revocation verified, manager signed in again, scoped read-only check passed at 02:48:18.909Z; no old-token reuse probe | [Security evidence](evidence/security-evidence.md) retains the incident. Explicit npm-audit authorization and other QA-12 evidence remain open; no clean-security claim | Agent / security follow-up |
| Post-run inventory and jobs | Verified by read-only metadata/results | Exact two datasets/five tables; raw rows10/10, serving rows10/10 plus one manifest; six top-level jobs and six children all DONE successfully, children zero billed bytes, total query usage50 MiB | [Metadata record](evidence/cloud-proof.json); no new query submitted. Earlier403 and corrected child-listing400 remain historical, not erased by later success | Agent / observed 2026-09-22T02:48:18.909Z |
| Terraform | Not Yet Verified | Binary reports v1.11.4; reading user configuration directory denied | Presence alone does not prove working infrastructure tooling; choose IaC proportionately at architecture | Agent / Gate 4 |
| GitHub repository read access | Verified | GitHub connector returned metadata for `McQueen-Cloud-Advisory/astra-analytics-delivery-lab`, default branch `main`, visibility public, pull/push permissions reported | Metadata read only; no commits, pushes, issues, PRs or settings changed | Agent / current |
| GitHub CLI / CI execution | Not Yet Verified | Initial `gh` configuration access denied and workflow-list connector request rejected; no executed CI result retained | Does not prove authentication failure or disabled Actions; verify permitted CI path and execute checks during approved build | Agent; manager for required external setup / Gates 4–6 |
| Existing Power BI project source | Verified as files only | User-provided `powerbi-files/placeholder-report.pbip`, PBIR report, TMDL model and one page; parsed metadata and source inspected | Empty starting model has no demonstrated source connection, measures, or report behavior; preserve it | Agent / current |
| Power BI Desktop/runtime | Build and normal launch established by user evidence; integration Not Run | User supplied August 2026 **2.157.1354.0 (26.08), x64** About details at 2026-09-21T03:46:04.6414546Z. Enabled: `PBI_gitIntegration`, `PBI_tmdlInDataset`, `PBI_enhancedReportFormat`, `PBI_enhancedReportFormatPBIX`; BigQuery legacy ODBC flag disabled. User confirms normal launch and no error message | C3-02 readiness is closed at Gate 4 using supplied evidence and the accepted human procedure. Actual PBIP/connector/open/refresh proof remains after Gate 5; no extra preliminary open test imposed | Manager, approved host verifier / first build slice |
| Power BI Service publication, capacity and workspace | N/A for current scope | Manager explicitly says the report will not be published, DEC-SCOPE-001; no service inspection needed for this endpoint | Exclude publication/capacity purchases; local Desktop connectivity, authentication and refresh still require verification | Agent retains scope; manager for any later change |
| New minimal Power BI proof source | Minimal host proof Pass | Manager confirmed both refreshes displayed 48/$144/10 including save/reopen; Desktop-added metadata and retained source bindings corroborate round-trip | [Desktop evidence](evidence/desktop-proof-validation.md); original metadata schema check remains historical, and a cache-excluding validator checks current source. Preserve user saves | Manager + agent / minimal proof closed |
| End-to-end BigQuery → Power BI behavior | Minimal proof Pass | Ten-line cloud slice plus actual host result and 18 successful connector queries in approved project/us-east4; pinned batch and no extra named resource | Broader filters, stale/error cases, buyer tasks and final report checks remain open; next platform slice is now permitted | Agent and manager / Gate 6 in progress |
| Docker executable | Found; runtime Not Yet Verified | Phase 4 Get-Command found Docker executable; daemon/container execution not tested | Does not require adopting a container route; current proposal uses Node locally | Agent / only if alternative selected |
| Native app control | Unavailable through current helper | Computer-use skill initialization imported successfully; `sky.list_apps()` failed: native pipe unavailable, OS error 2 | Cannot inspect Desktop version/render from this session; manager supplies host facts/evidence. No broad claim that Desktop/native APIs are absent | Manager for host checks; agent records limit |
| Browser control for cloud preflight | Unavailable in current session | `cua.getBrowser` for Cloud Console returned “No browser is available” | No console access/authentication/billing check occurred. Manager console or a supported authenticated CLI/API path is needed | Manager and agent / C3-01 |

Gate 5 is **Approved unchanged**, DEC-G5-001; C3-04 closes on the approved baseline. Verified billing/control configuration and manager-confirmed headroom supported the successful scoped-runtime cloud slice. Broader query/load/recovery acceptance and host proof remain open; no backend-validation gate is passed. The conventional ADC file is not used by this runtime; its earlier uninspected contents are not a prerequisite for explicit CLI impersonation. [Bootstrap evidence](evidence/cloud-bootstrap.md) preserves historical setup results; [cloud controls](evidence/cloud-controls.json) records prerequisite checks before provisioning, and [first-slice evidence](evidence/phase-6-cloud-proof.md) records executed workloads.

The token-exposure stop was addressed by explicitly authorized revocation, manager sign-in and safe scoped validation. Cloud readiness is resumed. No old-token reuse probe was made, and the historical exposure remains recorded. The current dependency is actual Desktop verification; explicit dependency-audit authorization and executed CI remain outstanding. Original resource/cost limits continue to apply.

The sole Human Manager is now the approved host verifier under charter v1.1. That role assignment is not completed verification or acceptance. Current native/browser helpers do not provide a usable host surface; retain actual human host evidence at the applicable gates.

Only relevant version/format facts from the user diagnostic are retained here. The full telemetry block, user/session identifiers and trace paths are unnecessary for the public repository and are not included. Although the copied block carried a Frown (Error) label, the manager clarified it came from About after normal Desktop launch, not from opening the placeholder, and **no error messages occurred**. No report defect or repair is inferred.

Command failures and connector limitations are retained as observations; they are not technical interventions by the manager. The original preflight made no cloud mutation. Later approved work created the dedicated project, scoped identity, controls and two datasets/five tables, then published/reconciled FIX-01. The initial billing failure and unexplained first process exit remain historical evidence; the manager's billing/authentication/headroom actions are not technical corrections. A successful bounded retry does not establish the cause of the earlier exit. No Desktop or browser-host action has been verified.
